{
  "name": "landing-page",
  "version": "1.0.0",
  "description": "Clean Code Challenges Landing Page",
  "main": "index.js",
  "author": "Edgar Pino ",
  "private": true,
  "scripts": {
    "start": "parcel ./src/index.html",
    "build": "parcel build ./src/index.html",
    "deploy": "rm -rf ./dist && yarn build && cp ./CNAME ./dist && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0",
    "parcel-bundler": "^1.9.7",
    "typescript": "^3.0.1"
  }
}